name: CI
on:
  push:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: 'Testing GitHub Action'
        run: pwd

      - name: 'Testing Printing'
        run: echo Hamza
